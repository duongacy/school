// Theme file stub removed - Chakra UI has been removed from this project.
// Keep an empty default export to avoid import errors in case any file still imports it.
const noop: Record<string, unknown> = {};
export default noop;
